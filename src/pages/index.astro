---
import { Card, CardHeader, CardTitle } from '@/components/ui/card';
import SearchChannels from '@/components/SearchChannels.jsx';
import Layout from '@/layouts/Layout.astro';
---

<Layout title="Canales de Streaming">
    <Card className="max-w-6xl mx-auto mt-8">
        <CardHeader>
            <CardTitle className="text-3xl font-bold text-center">Lista de canales de streaming</CardTitle>
        </CardHeader>
        <SearchChannels client:load />
    </Card>
</Layout>
